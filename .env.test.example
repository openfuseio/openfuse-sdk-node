# E2E Test Configuration
# Copy this file to .env.test and fill in the values

# SDK Client Credentials (from Keycloak backend client)
E2E_CLIENT_SECRET=your-client-secret-here

# Root User Credentials (for trip policy creation)
E2E_ROOT_USER_PASSWORD=your-root-user-password

# Optional overrides (defaults shown)
# E2E_API_BASE=https://prod--acme.api.localhost
# E2E_KEYCLOAK_URL=http://localhost:8080
# E2E_KEYCLOAK_REALM=local-openfuse-tenants
# E2E_COMPANY_SLUG=acme
# E2E_ENVIRONMENT_SLUG=prod
# E2E_ROOT_USER_EMAIL=rodrigo@openfuse.io
